import {
  StyleSheet,
} from 'react-native';

import {
  metrics,
} from 'sportunity/src/theme';

export default StyleSheet.create({
  container: {
    backgroundColor: '#eee',
    flex: 1,
    justifyContent: 'center',
  },
});
