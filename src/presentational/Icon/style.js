import { StyleSheet } from 'react-native';

const iconStyles = StyleSheet.create({
  keyIcon: {
    width: 90,
    height: 45,
    alignSelf: 'center',
  },
  centerIcon: {
    alignSelf: 'center',
  },
});

export default iconStyles;
